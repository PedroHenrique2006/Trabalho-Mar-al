<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjetoMauiApp1.Sobre"
             Title="SOBRE">
    <ScrollView>
        <StackLayout Padding="10" Spacing="10">
            <Label Text="Sobre o Aplicativo"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />
            <BoxView HeightRequest="2" Color="Gray" />
            <Label Text="Este aplicativo foi desenvolvido para fornecer informações sobre cursos e disciplinas e inserir dados de tais, Gerido pelo professor: Juliano Marcal."
                   FontSize="17"
                   HorizontalOptions="Center" />
            <Label Text="Sobre o Desenvolvedor"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalTextAlignment="Center" />
            <BoxView HeightRequest="2" Color="Gray" />
            <Label Text="Desenvolvido por: Pedro Henrique Miguel"
                   FontAttributes="Bold"
                   FontSize="16"
                   HorizontalOptions="Center" />
            <Label Text="RA: 2407862"
                   FontSize="16"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />
            <BoxView HeightRequest="2" Color="Gray" />
            <Image Source="logo_unianchieta.png" HeightRequest="100" HorizontalOptions="Center" />
            <Label Text="Versão: FaculAPP 1.0"
                   FontSize="16"
                   HorizontalOptions="Center" />
        </StackLayout>
    </ScrollView>
</ContentPage>